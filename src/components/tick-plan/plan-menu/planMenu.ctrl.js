(function () {
    'use strict';

    angular.module('acc')
        .controller('PlanMenuController', PlanMenuController);

    function PlanMenuController() {
        const ctrl = this;

        angular.extend(ctrl, {});

        ctrl.$onInit = function () {

        };
    }
})();